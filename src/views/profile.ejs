<%- include('partials/header') %>
<style>
    .item-img {
        width: 120px;
        height: 120px;
        object-fit: contain;
        object-position: center;
        background-color: #f8f9fa;
    }
</style>
<div class="container py-5">
    <div class="row g-5">
        <div class="col-lg-4">
            <img src="/img/<%= user.profile_image %>" class="img-fluid rounded shadow-sm w-100" alt="Profile Image">
            <div class="d-grid gap-2 mt-3">
                <button type="button" class="btn btn-outline-dark" data-user-id="<%= user.id %>">
                    Update Account
                </button>
                <button type="button" class="btn btn-outline-danger" data-user-id="<%= user.id %>">
                    Delete Account
                </button>
            </div>
        </div>
        <div class="col-lg-8">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="h3 mb-0">Your products</h2>
                <a href="/items/new" class="btn btn-dark">
                    Add item to sell
                </a>
            </div>
            <hr>
            <% items.forEach(item => { %>
            <div class="d-flex mb-4">
                <!-- Added the 'item-img' class here -->
                <img src="/img/<%= item.image_path%>" class="rounded me-3 item-img" alt="<%= item.name%>">
                <div>
                    <h3 class="h4 mb-2"><%= item.name%></h3>
                    <h5 class="text-success mb-2"></h5>
                    <p class="text-secondary mb-0">
                        Lorem ipsum dolor sit amet consectetur adipisicing elit.
                        Reiciendis vero deleniti dignissimos.
                    </p>
                    <div class="d-flex gap-2 mt-3">
                        <button type="button" class="btn btn-outline-dark btn-sm" data-item-id="<%= item.id %>">
                            Update Item
                        </button>
                        <button type="button" class="btn btn-outline-danger btn-sm" data-item-id="<%= item.id %>">
                            Delete Item
                        </button>
                    </div>
                </div>
            </div>
            <hr>
            <%});%>
        </div>
    </div>
</div>
<%- include('partials/footer') %>
<script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
</body>
</html>